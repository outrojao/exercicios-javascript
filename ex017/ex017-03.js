let v = function(x){ //armazenando uma function dentro de uma variável
    return x+2
}

console.log(v(2)) //passando um parâmetro para a variável para que a função seja executada (a qual esta dentro dela)